angular.module("MyCvTracker.pages.userManagement")
  .controller("UserManagementCtrl", [
    "toastr",
    "$rootScope",
    "$scope",
    "$injector",
    "$http",
    function (
      toastr,
      $rootScope,
      $scope,
      $injector,
      $http
    ) {

    }
  ]);
