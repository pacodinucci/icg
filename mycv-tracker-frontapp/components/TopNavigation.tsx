import React, {
  ReactElement
} from "react";
import {
  Container
} from "reactstrap";

const TopNavigation = (): ReactElement => {
  return (
    <header>
      <Container>
        Top Navigation
      </Container>
    </header>
  );
};

export default TopNavigation;
